# Docs site — in-repo documentation browser

React + Vite + xyflow. Served at http://localhost:5174 by `npm run dev`
(repo root); never deployed, local only.

Browse guides and architecture diagrams here; the decision records themselves
live as markdown under `docs/architecture/` (read them before changing
architecture — see `AGENTS.md`).
